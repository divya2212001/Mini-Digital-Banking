# Leave unset in local dev to use Vite's `/api` proxy → http://localhost:5000 (no CORS issues).
# Set only if the app must call the API directly across origins, e.g.:
# VITE_API_URL=http://localhost:5000/api
