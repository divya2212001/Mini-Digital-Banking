PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/mini-digital-banking
JWT_SECRET=change-this-to-a-long-random-string
JWT_EXPIRES_IN=7d

# Must match the browser origin when not using Vite proxy (e.g. explicit full API URL in frontend)
CLIENT_ORIGIN=http://localhost:5173

# Fraud rules
FRAUD_AMOUNT_THRESHOLD=10000
